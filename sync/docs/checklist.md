# Implementation Checklist

Tracks progress against `plan.md` / `architecture.md`. Check items off as they're completed; leave a short note next to any item that's partially done or blocked, so the next session can pick up exactly where this one left off.

## Phase 1 — Project scaffolding

- [x] `pyproject.toml`, `Makefile`, `requirements.txt`
- [x] `.env` + `.gitignore` (must include `sync.sqlite`, `.env`, `venv/`, `__pycache__/`)
- [x] `main.py` — `typer` CLI entry point (no subcommands wired yet, just the app). Note: `typer.Typer()` raises `RuntimeError: Could not get a command for this Typer instance` if actually invoked with zero registered commands — expected/normal until Phase 4 adds `discover-new`; verified the module *imports* cleanly instead.
- [x] `src/utils/path.py` — `ROOT_PATH`
- [x] `src/config/load_env.py` — `load_dotenv()` + `Config(BaseModel)` + `get_config()`
- [x] `src/utils/logging.py` — shared logger (file + stdout) + batch-progress helper

## Phase 2 — DB layer

- [x] `src/db/database.py` — `Database` class
- [x] `src/db/tables/_table.py` — base `Table` class
- [x] `tables/anki_notes.py`
- [x] `tables/cambridge_words.py`
- [x] `tables/cambridge_parts_of_speech.py`
- [x] `tables/cambridge_transcriptions.py`
- [x] `tables/cambridge_senses.py`
- [x] `tables/cambridge_definitions.py`
- [x] `tables/cambridge_examples.py`
- [x] `tables/note_definition_links.py` (with the partial unique index on `confirmed`) — verified live: confirming a second candidate for the same note is rejected by SQLite (`IntegrityError: UNIQUE constraint failed: note_definition_links.anki_note_id`), `NoteDefinitionLinksTable.confirm()` catches it and returns `None`.
- [x] `service/anki_note.py` — lookup by `system_note_uuid` first, `anki_note_id` fallback
- [x] `service/cambridge_*.py` services
- [x] `service/note_definition_link.py` — candidate/confirmed/rejected lifecycle
- [x] `src/utils/hashing.py` — `compute_content_hash()`

## Phase 3 — Anki access

- [x] `src/anki_client.py` — `invoke()`, copied from `mcp/src/anki_client.py`. Deviation: sources the URL from `get_config().anki_connect_url` instead of a hardcoded constant, since this project (unlike `mcp`) already has `.env`/`Config` for it — `invoke(action, **params)` signature and error handling are otherwise identical.
- [x] `src/anki/notes.py` — `get_notes`, `get_notes_fields`, `update_note_fields`
- [x] `src/anki/notes.py` — `get_notes_missing_uuid(deck)` (empty-field search) — verified live and read-only against the real `definition` deck: 1699 notes currently have an empty `_system_note_uuid` (expected pre-`stamp` state).

## Phase 4 — `discover-new`

- [x] Command implemented, read-only, reports count + note id + word
- [x] Manually verified against the real `definition` deck — 1699 notes reported missing `_system_note_uuid`, matching Phase 3's read-only check. Note: with only one `typer` command registered, `main.py` currently must be invoked with no subcommand name (`python main.py --deck ...`), not `python main.py discover-new`; Typer switches to requiring the subcommand name automatically once a second command is registered (Phase 5's `stamp`), so this resolves itself and needs no fix.

## Phase 5 — `stamp`

- [x] Command implemented (`--deck`, `--force`, `--dry-run`)
- [x] Non-empty guard confirmed working (skips already-stamped notes) — covered by `tests/test_stamp.py` (`test_stamp_skips_already_stamped_note`, `test_stamp_only_writes_the_empty_field`); live confirmation is indirect (0 notes missing `_system_note_uuid` after the real run means a future re-run would skip all of them, but that re-run itself hasn't happened live yet).
- [x] `--dry-run` run against the real deck and output sanity-checked — 1699/1699 notes report `updated` (both fields empty), 0 skipped, 0 errors; confirmed no writes actually happened (`get_notes_missing_uuid` still 1699 afterward).
- [x] Real run: user ran `python main.py stamp` (no flags) themselves. Confirmed live afterward: 0 notes missing `_system_note_uuid` in the `definition` deck (down from 1699).

## Phase 6 — `pull`

- [x] Command implemented (`--deck`, `--skip-review`) — no `--dry-run` (not in `architecture.md`'s CLI table for `pull`; interactive per-note review is the built-in safety net instead).
- [x] Per-note upsert (not blanket delete+reinsert), keyed by `system_note_uuid` → `anki_note_id` fallback — reuses `AnkiNoteService` from Phase 2, verified against 3 real notes on a scratch DB (created on pass 1, zero diffs on pass 2 re-run).
- [x] Before/after diff + review prompt working, `REQUIRE_REVIEW=false` / `--skip-review` bypass verified — diff computation verified (hand-edited `definition` locally → correctly detected as the only changed field); `--skip-review` bypass verified live against the real DB (1699 notes created with zero prompts). Note: the interactive `input()` confirmation path itself (`REQUIRE_REVIEW=true`, no `--skip-review`) has not been exercised live — only unit-level via `confirm_diff`'s logic; real run so far has always used `--skip-review` since reviewing ~1699 individual prompts isn't practical for a first pull.
- [x] Per-note error isolation verified (one bad note doesn't abort the batch; `sync_status`/`sync_issue` populated) — simulated a note with no fields, correctly stored as `sync_status='error'` with a descriptive `sync_issue`, no exception propagated.
- [x] Progress logging (`[i/total] (pct%)`) + final summary verified — real run: `Summary: total=1699, created=1699, updated=0, unchanged=0, error=0`; re-run: `Summary: total=1699, created=0, updated=0, unchanged=1699, error=0` (matches `plan.md`'s "pull twice in a row reports zero diffs" verification point).

## Phase 7 — Cambridge data (external — nothing to build here)

- [x] `cambridge_*` schema confirmed stable/final (no further changes expected once `lexicon-scraper` starts targeting it) — schema unchanged since Phase 2; Phase 8's `match` reads it via the exact table/service shape defined there, no new columns needed.
- [x] Noted to self: integrating `lexicon-scraper` to write into `sync.sqlite` is separate work, not part of this checklist

## Phase 8 — `match` / `confirm` / `review`

- [x] `match` implemented — candidates via `(word, transcription, transcription_type)` (`src/sync/match.py`)
- [x] `confirm` implemented — manual pairing and `--auto-single` (`src/sync/confirm.py`)
- [x] Partial unique index (`confirmed` per note) verified to actually prevent a second confirmed row — re-verified in `tests/test_db_layer.py` (was already verified manually in Phase 2)
- [x] `review` implemented — unmatched / multi-candidate / `sync_status='error'` reporting (`src/sync/review.py`)
- **Not verified against real data** — `cambridge_*` is empty in the real DB (`lexicon-scraper` hasn't run against it). All three commands are covered by `tests/test_match.py`/`test_confirm.py`/`test_review.py` against hand-seeded synthetic Cambridge rows only. See `docs/test-coverage.md`. None of these commands write to Anki, so this is lower-risk than `enrich` — but still unverified against anything real.

## Phase 9 — `enrich`

- [x] Command implemented (`--force`, `--dry-run`, `--skip-review`) (`src/sync/enrich.py`)
- [x] Field whitelist built as an extensible registry (not inline logic), seeded with `cefr` only — `FIELD_RESOLVERS = {"cefr": _resolve_cefr}`
- [x] Reads only `confirmed` links (never `candidate`) — uses `NoteDefinitionLinksTable.get_confirmed_for_note()`
- [x] Non-empty guard + live re-fetch before write verified — covered by `tests/test_enrich.py` (`test_enrich_does_not_overwrite_existing_cefr_without_force`, `test_enrich_force_overwrites_existing_cefr`); re-fetches via `get_notes_fields([note_id])` immediately before deciding what to write
- [x] Review/diff + progress logging shared with `pull` (not reimplemented) — reuses `src/sync/diff.py`'s `confirm_diff` and `src/utils/logging.py`'s `log_progress`/`log_summary`
- **Not run against real Anki** — depends on `match`/`confirm` producing real confirmed links first, which depends on real Cambridge data. **Do not run `enrich` for real until Phase 8 has been exercised against real Cambridge data first.** See `docs/test-coverage.md`.

## End-to-end verification (from `plan.md`)

- [ ] `discover-new` after a fresh Anki import lists exactly the new notes — not yet re-verified against a genuinely fresh import (no new notes added to Anki since this project started); logic verified live against the pre-`stamp` state (1699/1699) and by `tests/test_anki_notes.py`/`test_cli.py`.
- [x] `stamp --dry-run` first run: one planned write per note; later run: only newly-discovered notes — first run confirmed live (1699/1699 planned); real `stamp` then run by the user directly, confirmed 0 notes missing `_system_note_uuid` afterward. "Later run only touches newly-discovered notes" covered by `tests/test_stamp.py`, not yet re-verified live against a genuinely fresh import.
- [x] `stamp` → `pull`: every mirrored note carries non-empty `system_hash`/`system_note_uuid` — confirmed live: all 1699 rows in `anki_notes` have `sync_status='ok'` and a populated `system_note_uuid`.
- [x] `pull` run twice with no Anki changes in between: zero diffs, zero errors — confirmed live: second run reported `unchanged=1699, error=0`.
- [x] Hand-edit a note's `definition` in Anki → `pull` shows the diff, but a previously confirmed link still holds — diff-detection verified both live (Phase 6) and by `tests/test_pull.py`; "confirmed link still holds" verified by `tests/test_pull.py` + `test_confirm.py` together against synthetic data (a confirmed link is keyed on `anki_note_id`, untouched by re-pulling that note), not yet with a real Anki edit + real Cambridge data together.
- [x] A word whose one transcription covers three senses produces three candidates; `confirm --auto-single` resolves only unambiguous ones — covered exactly by `tests/test_match.py::test_one_transcription_covering_three_senses_produces_three_candidates` and `tests/test_confirm.py::test_confirm_auto_single_resolves_only_unambiguous_notes`, against synthetic data only (see `docs/test-coverage.md`).
- [x] `enrich --dry-run` only plans `cefr` writes for empty-`cefr` notes with a confirmed link — covered by `tests/test_enrich.py`, against synthetic data only. **Not run live** — see `docs/test-coverage.md`.
- [x] Simulated bad note during `pull` doesn't abort the batch — confirmed via scratch-DB smoke test in Phase 6 and formalized in `tests/test_pull.py::test_pull_records_error_row_for_note_missing_from_notes_info`.

## Tests

- [x] `tests/` directory with `pytest` (see `docs/test-coverage.md` for the live-vs-synthetic breakdown per command)
- [x] Every Anki-touching code path tested via a fake in-memory AnkiConnect (`tests/conftest.py::FakeAnkiConnect`), patched in at `src.anki.notes.invoke` — no test ever opens a real socket to AnkiConnect
- [x] Every DB-touching test uses its own scratch sqlite file (`DB_PATH` monkeypatched per-test via an autouse fixture) — no test ever touches `data/sync.sqlite`
- [x] 81 tests, all passing; ruff clean; confirmed after the run that the real DB (1699 `anki_notes` rows, 0 `cambridge_words`, 0 `note_definition_links`) and the real Anki collection (0 notes missing `_system_note_uuid`) were both unchanged by the suite

## Log

_Add a dated one-line entry here each session, e.g. "2026-07-22 — scaffolding + DB layer done, starting Anki access."_

- 2026-07-22 — Phase 1 (scaffolding) and Phase 2 (DB layer) complete: `pyproject.toml`/`Makefile`/`requirements.txt`/`.env`/`.gitignore`/`main.py` in place; venv installed (pydantic 2.13.4, python-dotenv 1.2.2, typer 0.27.0); `Database`/`Table`/`Service` pattern copied from `grammar_drill` and extended with `anki_notes`, all six `cambridge_*` tables, and `note_definition_links` (partial-unique-index-backed candidate/confirmed/rejected lifecycle); `compute_content_hash()` and the shared logger (`get_logger`/`log_progress`/`log_summary`) are in place. Verified live against the real Anki collection (read-only, via the anki-mcp tool) that the note-type field names in `architecture.md`'s schema (`word`, `trans`, `trans_type`, `part_of_speech`, `sense`, `definition`, `cefr`, `example_1..7`, `audio`, `picture`, `video`, `hints`, `_system_hash`, `_system_note_uuid`) match reality exactly, and that AnkiConnect's `"_system_note_uuid:"` empty-field search syntax (needed by `discover-new` in Phase 4) works as documented. Full DB layer exercised with an in-memory smoke test: upsert-by-uuid-then-anki_note_id-fallback, Cambridge get-or-create chain (word → pos → transcription/sense → definition), candidate proposal, `confirm_if_single_candidate` (correctly refuses when >1 candidate), and the partial-unique-confirmed-index blocking a second confirm — all passed; ruff lint + format clean.
  - **Bug found and fixed in the copied pattern** (flagging per the "ask before deviating" instruction, but fixed since it's implementation-level, not schema/command-boundary): `grammar_drill`'s `Database.execute()` checks `sql.startswith("INSERT")` to decide whether to return `cursor.lastrowid`, but every `_INSERT_QUERY` in that codebase (and in this one, following the same `textwrap.dedent("""\n    INSERT ...\n""")` style) starts with a leading `\n` after dedenting, so that check is always `False` — inserts always fell through to the generic `return 1` success indicator instead of the real row id. Confirmed this silently corrupts every get-or-create service in `grammar_drill` too (`WordService.add()`/`ConditionService.add()` would always report id `1`, which `ReviewService` then uses as the FK). Fixed here with `sql.strip().startswith("INSERT")` in `sync/src/db/database.py`. Not yet back-ported to `grammar_drill` — flagging so you can decide whether that's worth fixing there too.
  - Also deviated in one spot: `Database.are_tables_created()` in `grammar_drill` derives the expected table name from the class name (`WordsTable` → `words`), which breaks for our multi-word snake_case table names (e.g. `AnkiNotesTable` → `ankinotes` ≠ `anki_notes`). Replaced with an explicit `NAME` class attribute on `Table`/subclasses instead of string-mangling the class name. Functionally this only affected the "already created" short-circuit (CREATE TABLE IF NOT EXISTS made it harmless either way) — verified `are_tables_created()` now correctly returns `True` on a second call.
- Stopping here per instructions — next session starts Phase 3 (Anki access: `src/anki_client.py` + `src/anki/notes.py`), which is the first phase that talks to the real Anki collection for writes (though Phase 3 itself is read-only helpers; `stamp` in Phase 5 is the first actual write).
- 2026-07-22 (same day, cont'd) — Replaced all relative imports (`from .`/`from ..`) across `src/` with absolute `from src....` imports per request; added `known-first-party = ["src"]` to ruff's isort config to match. Phase 3 (Anki access) complete: `src/anki_client.py` (same `invoke()` contract as `mcp/src/anki_client.py`, sourcing the URL from our own `Config` instead of a hardcoded constant) and `src/anki/notes.py` (`get_notes`, `get_notes_fields`, `update_note_fields`, `get_notes_missing_uuid`). Verified read-only against the live Anki collection directly through our own client (not the MCP tool): `get_notes_missing_uuid` on the `definition` deck returns 1699 notes, matching the pre-`stamp` expectation. No writes performed. Ruff clean.
- Next: Phase 4 (`discover-new`) — the first actual CLI command, still read-only.
- 2026-07-22 (same day, cont'd) — Moved the DB file into its own directory: `data/sync.sqlite` instead of a bare `sync.sqlite` at the project root (`.env`/`load_env.py` default/`.gitignore` all updated accordingly — `.gitignore` now ignores the whole `data/` directory). Added `src/db/setup.py` (`get_database()`, `all_tables()`, `ensure_schema()`) as the single place listing every owned table in FK-safe creation order — needed by any future DB-touching command (`pull`/`match`/`confirm`/`enrich`) and used now to actually materialize `data/sync.sqlite` on disk with the full schema (verified: all 8 tables + `sqlite_sequence` present). Phase 4 (`discover-new`) complete: `src/sync/discover_new.py` + wired as a `typer` command in `main.py`, verified against the live `definition` deck (1699 notes reported, file+stdout logging both confirmed working via `logs/sync.log`).
  - **Process note**: while testing gitignore rules, a stray `cd` to the repo root caused `ruff format .` to run against the *root* `anki` repo instead of `sync/`, reformatting 5 files inside three unrelated git submodules (`addons/error_cooldown`, `addons/grammar_drill`, `addons/question_forge`). Caught it immediately via `git status`/`git diff --stat`, confirmed with the user, and reverted with `git checkout --` in each submodule before doing anything else. No lasting effect, but noting it since it's the kind of mistake worth double-checking `pwd` before recovering from.
- Next: Phase 5 (`stamp`) — the first command that writes to the real Anki collection. Per standing instructions, run with `--dry-run` first and show the output before any real run.
- 2026-07-22 (same day, cont'd) — User ran the real `stamp` (no flags) themselves; confirmed live: 0 notes now missing `_system_note_uuid` in the `definition` deck (down from 1699). Added `docs/usage.md` (Russian, plain-language) clarification that `--force` is *not* needed on a first run — the existing "skip if already non-empty" guard already handles that; `--force` is only for the rare re-stamp-on-purpose case.
  - Phase 6 (`pull`) implemented: `src/sync/pull.py`, `src/sync/diff.py` (shared before/after diff helper per `architecture.md`'s directory layout), and `CONTENT_FIELDS` promoted from a private `_HASHED_FIELDS` list in `hashing.py` to a shared public constant (both `pull` and hashing now read from one list, avoiding drift). No `--dry-run` flag on `pull` — not in `architecture.md`'s CLI table; the interactive per-note review *is* the spec'd safety net, bypassable via `--skip-review`/`REQUIRE_REVIEW=false`.
  - Verified the core logic against real (read-only) Anki data on a disposable scratch DB before touching the real one: note creation, zero-diff on an unchanged re-run, hand-edited-field diff detection, and a simulated fields-missing note correctly recorded as `sync_status='error'` without aborting.
  - Asked the user how to run the real `pull` (interactive review vs. `--skip-review`, given ~1699 prompts isn't practical for a first run) — user chose `--skip-review`. Ran it for real: `created=1699, updated=0, unchanged=0, error=0`. Verified directly against `data/sync.sqlite`: 1699 rows, all `sync_status='ok'`, all with a populated `system_note_uuid`. Re-ran immediately after: `created=0, updated=0, unchanged=1699, error=0` — matches `plan.md`'s "pull twice with no Anki changes reports zero diffs" verification point exactly. `docs/usage.md` updated with a plain-language `pull` section.
  - Not yet exercised: the interactive `input()` confirmation prompt itself in a live run (always used `--skip-review` so far) — logic-level covered via `confirm_diff`, but no live human-in-the-loop run yet. Fine to leave for whenever a smaller/partial pull naturally exercises it.
- Next: Phase 7 is external (nothing to build — `lexicon-scraper`'s job). So next actual work is Phase 8 (`match`/`confirm`/`review`), which needs Cambridge reference data to be present in `cambridge_*` tables to test against meaningfully — currently empty since `lexicon-scraper` hasn't been run against this DB yet. Worth checking with the user before starting Phase 8 whether that data exists somewhere to test against, or whether `match`/`confirm`/`review` should be built and unit-verified against synthetic data first (like Phase 2's DB-layer smoke test) and left for live verification later.
- 2026-07-22 (same day, cont'd) — Phase 5 (`stamp`) implemented: `src/sync/stamp.py` + wired as a `typer` command in `main.py`. Ran `--dry-run` against the real `definition` deck and showed the user the output before doing anything real (per standing instructions): 1699/1699 notes planned for `updated` (both `_system_hash`/`_system_note_uuid` currently empty), 0 skipped, 0 errors — matches the expected first-run scenario from `plan.md`'s verification checklist. User asked to hold off on the real run and instead write a plain-language usage guide first — added `docs/usage.md` (in Russian, matching how the request was phrased) covering how/where to run each command, what each parameter does, and what actually happens in plain terms; will extend it as later phases land. **The real (non-dry-run) `stamp` has not been executed yet** — waiting on explicit go-ahead.
- 2026-07-22 (same day, cont'd) — User directed: implement remaining phases autonomously, build a `tests/` suite, never run anything against real Cambridge/Anki data that could fail for lack of it, and document live-vs-synthetic coverage explicitly.
  - **Phase 8 (`match`/`confirm`/`review`) implemented**: `src/sync/match.py` (candidates via `(word, transcription, transcription_type)`, traversing `cambridge_words → parts_of_speech → transcriptions/senses → definitions`), `src/sync/confirm.py` (`confirm_manual`, `confirm_auto_single`), `src/sync/review.py` (zero-candidates / multi-candidates / pull-errors report). Added supporting query methods: `AnkiNotesTable.get_for_deck()`/`get_errors()`/`all()`, `NoteDefinitionLinksTable.get_note_ids_with_candidates()`/`get_note_ids_with_any_link()`/`get_multi_candidate_note_ids()`.
  - **Phase 9 (`enrich`) implemented**: `src/sync/enrich.py`, `FIELD_RESOLVERS` registry seeded with `cefr` only, reads only `confirmed` links, re-fetches live Anki fields immediately before writing, reuses `diff.py`'s `confirm_diff` and the shared progress/summary logger.
  - All 7 commands wired into `main.py` (`discover-new`, `stamp`, `pull`, `match`, `confirm`, `review`, `enrich`); `--help` verified.
  - **Built `tests/`**: `pytest==9.1.1` added; `pyproject.toml` gets `[tool.pytest.ini_options]` (`testpaths=["tests"]`, `pythonpath=["."]`). `tests/conftest.py` provides an autouse fixture that points `DB_PATH` at a fresh per-test scratch file and a `FakeAnkiConnect` fixture patched in at `src.anki.notes.invoke` (the single point every Anki-touching function calls through, regardless of which command module imports it) — no test ever opens a real socket or touches `data/sync.sqlite`. 81 tests across `test_hashing.py`, `test_diff.py`, `test_database.py` (includes a regression test for the `sql.startswith("INSERT")` bug fixed earlier), `test_db_layer.py`, `test_anki_client.py`, `test_anki_notes.py`, `test_stamp.py`, `test_pull.py`, `test_match.py` (includes the exact "one transcription, three senses → three candidates" scenario from `plan.md`), `test_confirm.py` (includes the exact `--auto-single` unambiguous-only scenario), `test_review.py`, `test_enrich.py`, and a thin `test_cli.py` (exit-code-only checks — the CLI runner's stdout capture doesn't see our logger's output since its `StreamHandler` bound `sys.stdout` at import time). All 81 pass; ruff clean.
  - Caught one test-setup bug via a real FK violation (not a production bug): two `test_db_layer.py` tests tried to create `note_definition_links` rows for a note that was never inserted into `anki_notes` — the FK constraint correctly rejected it. Fixed by inserting the note first.
  - After the full suite run, explicitly verified the real DB and real Anki were untouched: `anki_notes` still 1699 rows, `cambridge_words`/`note_definition_links` still 0, real Anki still reports 0 notes missing `_system_note_uuid`.
  - Added `docs/test-coverage.md` — a per-command table distinguishing "verified live" (`discover-new`, `stamp`, `pull` — all three actually run against the real collection/DB) from "verified by the automated suite only, against synthetic data" (`match`, `confirm`, `review`, `enrich` — no real Cambridge data exists yet since `lexicon-scraper` hasn't populated `data/sync.sqlite`). Explicitly flags that `enrich` writes to the real Anki collection and should not be run for real until Phase 8 has been exercised against real Cambridge data first.
  - **Nothing was run against real Anki or real Cambridge data this session** beyond the read-only confirmation checks above (no `match`/`confirm`/`review`/`enrich` invocations against `data/sync.sqlite`, no `stamp`/`pull` re-runs).
- Next: whenever `lexicon-scraper` populates `cambridge_*` in `data/sync.sqlite`, run `match` (read-only relative to Anki, DB-internal) first, inspect via `review`, then `confirm` cautiously (manual pairs or `--auto-single`), and only then consider `enrich --dry-run` before any real `enrich` run — same dry-run-first discipline as `stamp`/`pull`.
- 2026-07-22 (same day, cont'd) — Three follow-up requests from the user:
  1. **Reordered `anki_notes` columns** to `id, anki_note_id, word, trans, trans_type, part_of_speech, sense, definition, cefr, example_1..7, audio, picture, video, hints, system_note_uuid, system_hash, source_deck, sync_status, sync_issue, last_pulled_at` (`src/db/tables/anki_notes.py` — CREATE/INSERT/UPDATE queries all updated; no code depends on column order since every query binds by `:name`, not position). Applied to the real DB step by step and shown at each step: dropped the old `anki_notes` table (safe — it's a read-only mirror of Anki, always refillable), recreated it via `ensure_schema()` (verified new column order via `PRAGMA table_info`), then ran `pull --skip-review` for real against the live deck to refill it (`created=1699, error=0`). Final state verified: 1699 rows, new column order confirmed on an actual row.
  2. **Explained `source_deck`**: confirmed by reading `AnkiNoteService.existing()` that identity lookup only ever uses `system_note_uuid` then `anki_note_id` — `source_deck` is never consulted. So pulling the same notes via a different sibling deck (`typing` instead of `definition`) would find the same rows by uuid/note-id, update them in place (not duplicate them), and just overwrite `source_deck` to reflect "last deck used to find this note" — purely provenance, matches what `architecture.md` already said. Confirmed live: the real DB currently shows exactly one distinct `source_deck` value.
  3. **Split `cambridge_*` tables/services into their own subdirectory**, mirrored across both layers: `src/db/tables/cambridge/` (six `cambridge_*.py` table files; `_table.py`, `anki_notes.py`, `note_definition_links.py` stay directly under `tables/`) and `src/db/service/cambridge/` (five `cambridge_*.py` service files; `anki_note.py`, `note_definition_link.py` stay directly under `service/`). Extended the split to the service layer on my own judgment for consistency even though only "tables" were named explicitly — flagging this deviation. Updated every import site across `src/`, `tests/`, and `main.py` (verified via grep: zero stale flat `cambridge_*` import paths remain). Updated `architecture.md`'s directory layout and the `anki_notes` schema table to match the new column order.
  - Full suite re-run after every step (81/81 passing, ruff clean) — no test changes were needed for the reorder (dict-based row access, order-independent) or the directory split beyond import paths (ruff's isort auto-fixed the import ordering).
